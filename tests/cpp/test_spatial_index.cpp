#include <gtest/gtest.h>
#include "maptitude/SpatialIndex.h"

// SpatialIndex tests require OEMolBase objects.
// These tests will be populated once OEChem is available in the test environment.

using namespace Maptitude;

// Placeholder test to verify compilation
TEST(SpatialIndexTest, Placeholder) {
    // SpatialIndex requires a molecule for construction.
    // Full tests will create OEGraphMol objects with known coordinates.
    SUCCEED();
}
